[defaults]
inventory = inventory/localhost.yml
roles_path = roles
host_key_checking = False
retry_files_enabled = False
nocows = 1
interpreter_python = auto_silent
# Fresh Ubuntu Server installs can be slow to present sudo prompts during fact gathering.
timeout = 60
